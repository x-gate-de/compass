# -----------------------------------------------------------------------------
# Datei: requirements.txt
# Autor: Torben <github@x-gate.de>
# Lizenz: AGPL-3.0-or-later — siehe LICENSE.
# Zweck:
# - Gepinnte Python-Abhaengigkeiten fuer compass (Daemon + Web-UI).
#   Vereint die Abhaengigkeiten von x-gate_chat (XMPP/OMEMO) und x-gate_nextup
#   (Aggregator/Connectoren) und ergaenzt die Grafana-Anbindung.
# Betriebs- und Wartungshinweise:
# - Vor produktivem Einsatz mit `pip-audit` auf Schwachstellen pruefen.
# - Nach Installation auf dem Host per `pip freeze` verifizieren.
# - Python >= 3.11 erforderlich (slixmpp 1.16); Zielserver Python 3.13.
# - IMAP/Odoo nutzen die Standardbibliothek (imaplib, xmlrpc.client) - keine Extra-Pakete.
# -----------------------------------------------------------------------------

# --- Web-UI (gemeinsam) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.32
itsdangerous==2.2.0

# --- Konfiguration + Verschluesselung (gemeinsam) ---
PyYAML==6.0.2
# Fernet fuer gespeicherte XMPP-Passwoerter und Feed-Credentials.
cryptography==48.0.1

# --- Async-HTTP: LLM-Aufrufe (Ollama) + Grafana-API ---
httpx==0.28.1

# --- XMPP-Core (asyncio) + OMEMO (aus x-gate_chat) ---
slixmpp==1.16.0
# HTTP File Upload (XEP-0363) fuer Anhang-Uploads.
aiohttp==3.14.1
# Web Push (VAPID) fuer Push-Benachrichtigungen.
pywebpush==2.3.0
# OMEMO (XEP-0384) Plugin + Krypto-Unterbau (Syndace-Familie).
slixmpp-omemo==2.2.0
OMEMO==2.1.0
twomemo==2.1.0
oldmemo==2.1.0
DoubleRatchet==1.3.0
X3DH==1.3.0
XEdDSA==1.2.0

# --- CalDAV-Connector (aus x-gate_nextup; zieht requests, lxml, vobject) ---
caldav==2.2.6
icalendar==6.3.2
